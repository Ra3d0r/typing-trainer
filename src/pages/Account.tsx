import UserScore from '@feature/userScore/UserScore';

const Account = () => {
	return (
		<div className="max-w-screen-xl w-full lg:w-3/5 mx-auto overflow-hidden">
			<UserScore />
		</div>
	);
};

export default Account;
